<header class="container-fluid">
    {{> navbar}}

    <div class="header__hero">
        <img src="{{bgimg}}" alt="">
        
        <div class="header__titulos">
            <h1>{{titulo}}</h1>
            <h3>{{subtitulo}}</h3>
        </div>
    </div>
</header>